import { test, describe, expect, it } from "vitest";
import Synkv from "../";


test("Synkv tests", () => {
  describe("sample test", () => {
    it("works", () => {
      expect(true).toBeTruthy();
    });
  });
});